// Exercício 7: Declare três variáveis e atribua valores numéricos a elas. Use operadores de comparação para comparar os valores entre as variáveis. Imprima os resultados. Exemplos de operadores de comparação: >, <, >=, <= e outros.


let numX = 1;
let numY = 2;
let numZ = 3;

console.log('NumX e maior que numY? ' + (numX > numY));
console.log('NumX e menor que numY? ' + (numX < numY));
console.log('NumX e maior ou igual a numZ? ' + (numX >= numZ));
console.log('NumX e menor ou igual a numZ? ' + (numX <= numZ));
console.log('NumZ e igual a numY? ' + (numZ == numY));